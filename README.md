# DL-Lib
A deep learning classification library built from scratch in C. Includes tanh, sigmoid, and PReLU activation functions (applies softmax on the last layer). Uses cross entropy to compute loss.
